{
  "fixedSectionOk": true,
  "nodesRecovered": 0,
  "edgesRestored": 0,
  "crossBatchEdgesAdded": 0,
  "typesRemapped": 0,
  "complexityRemapped": 0,
  "notes": [
    "Incremental merge verified clean: 325 nodes / 1339 edges, 0 dangling edges, 0 duplicate node IDs, 0 nodes missing id/type/summary/tags/complexity, 0 degraded placeholder nodes.",
    "All 25 re-analyzed files integrated cleanly: 19 Go/data file nodes + 6 doc nodes all present in the assembled graph.",
    "5 re-analyzed nodes have zero incoming edges (document:CHANGELOG.md, document:CLAUDE.md, document:README.md, document:docs/adr/0012-..., file:test/e2e/calibration_determinism_test.go). All are legitimate graph roots (top-level docs that document others; an e2e test that imports but is imported by nothing). Not lost edges - confirmed against batch sources, they appear only as edge sources, never targets.",
    "tested_by direction correct on all 43 edges (production source -> test target); 0 reversed/test-test/prod-prod. Matches merge report (2 invalid dropped, 6 path-convention linked).",
    "documents edges: all 25 have a document-type source pointing to the file/doc they describe; targets semantically sensible (theory/ADR/empirical docs map to the matching kernel source files).",
    "Cross-batch imports complete: all 999 import relationships in importMap (175 entries) have a corresponding imports edge; 0 missing, every referenced path exists as a file node. The 363 recovered + existing edges reconcile to the 999 imports-edge total. High imports count is expected (package-to-all-files resolution including _test.go), not flagged as error.",
    "No upstream ID-correction or complexity-skew concern: complexity distribution moderate 154 / simple 124 / complex 47 is plausible; node types all valid (function/file/document/class/config/service).",
    "No manual recovery, remapping, or edge restoration was required - the 'Could not fix' section was effectively empty (only 2 correctly-dropped invalid tested_by edges)."
  ]
}
